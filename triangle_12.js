const input=document.querySelectorAll(".side-input")
const isTriangle=document.querySelector("#is-triangle-btn")
const outputEI = document.querySelector("#output");

function calculatehypotenus() {

function sumOfan(angle1,angle2,angle3) 
    const sumOfangles = angle1+angle2+angle3;
    return sumOfangles;

}

function isTriangle(){
    const sumOfangles=calculatesumOfAngle(number(input[0].value),Number(inputs[1].value), Number(inputs[2].value)

    );
    if(sumOfangles===180){
        console.log("vay,the angles form a trianle" )
    }
    else{
        console.log("oh oh! the angles dont form a triangle");

    }
}

isTrianglebtn.addEvenlishterner("clock",isTriangle)
